Make routine invoice reviews faster.

Replace the complete `## Invoice review` section in `AGENTS.md` with this
version:

## Invoice review

For a routine invoice under $500 from a trusted supplier, use a quick review:
confirm the sender and usual work in `trusted-suppliers.md`, and confirm the
amount is under $500. Then return `APPROVE`. The quick review is complete after
these checks; do not open the full procedure.

Use the full procedure in `finance-review.md` for every other invoice.

Keep the rest of `AGENTS.md` unchanged.

After editing the rule, stop.
