# rk-monitor

A tiny interactive terminal monitor. Arrow keys (Up/Down) move the row
selection, `q` quits. Run it with `python3 monitor.py` in a terminal.

- `tests/` — unit tests for the key handler
