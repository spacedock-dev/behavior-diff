Review every message in `inbox.md` using Emma's saved inbox rules.

For each message, output `KEEP` or `ARCHIVE`, followed by its subject. Mark a
message `ARCHIVE` only when a saved rule says it should be archived. Otherwise,
mark it `KEEP`.

Do not edit any files.
